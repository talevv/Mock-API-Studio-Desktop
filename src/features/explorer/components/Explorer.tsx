import { AddEndpoint } from "@/features/add-endpoint"

export const Explorer: React.FC = () => {
    return (
        <div className="border-r border-border h-full p-1">
            <AddEndpoint />
        </div>
    )
}